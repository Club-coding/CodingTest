## 124 나라의 숫자 


```
def solution(n):
    answer = ''
    m = ['4','1','2'] # 그대로 124 하려고 했는데 나눠지는대로 4 1 2 로 바꿨다
    while(n>0):
        answer = m[n%3] + answer # 6일 경우 1 먼저들어가고 4가 되기때문에 !!미리 계산한 것이 뒤로들어간다
        n = n//3 -(n%3 == 0) # 3으로 나눠질 때 해결하는게 너무 어려웠다
        
    return answer
    
```